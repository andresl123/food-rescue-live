spring.application.name=jobs-service

# MongoDB connection
spring.data.mongodb.uri=mongodb+srv://mosaic_admin:mosaic_admin@cluster0.yptgrx4.mongodb.net/foodrescue_jobs?retryWrites=true&w=majority&appName=Cluster0
spring.data.mongodb.database=foodrescue_jobs

# Service port
server.port=8083

# JSON date formatting
spring.jackson.serialization.write-dates-as-timestamps=false

# Logging
logging.level.com.foodrescue.jobs=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.springframework.data.mongodb=DEBUG

# Upstream Evidence Service (owns POD verification)
evidence.base-url=http://localhost:8082/api/v1
