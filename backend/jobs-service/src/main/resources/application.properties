spring.application.name=jobs-service

# MongoDB connection (update with your real credentials/URI)
spring.data.mongodb.uri=mongodb+srv://mosaic_admin:mosaic_admin@cluster0.yptgrx4.mongodb.net/foodrescue_evidence?retryWrites=true&w=majority&appName=Cluster0

# Service port
server.port=8083

# JSON date formatting
spring.jackson.serialization.write-dates-as-timestamps=false

# Logging
logging.level.com.foodrescue.jobs=DEBUG
logging.level.org.springframework.web=INFO

# Upstream Evidence Service (owns POD verification)
evidence.base-url=http://localhost:8082/api/v1
spring.application.name=jobs-service
